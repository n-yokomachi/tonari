################################################################################
# Scensei 環境変数（シークレット・環境固有設定のみ）
# Environment Variables (Secrets & Environment-specific Only)
#
# アプリケーション設定 → config/app.json
# インフラ設定 → config/infra.json
# AgentCore設定 → config/agentcore.json
################################################################################

#===============================================================================
# 必須設定 / Required Settings
#===============================================================================

# Cognito クライアントシークレット（AgentCore認証用）
# Cognito Client Secret (for AgentCore authentication)
COGNITO_CLIENT_SECRET=

# 香水API URL（API Gateway）- CDKデプロイ後に設定
# Perfume API URL (API Gateway) - Set after CDK deployment
PERFUME_API_URL=

#===============================================================================
# オプション: 認証設定 / Optional: Authentication
#===============================================================================

# ベーシック認証（設定しない場合は無効）
# Basic Authentication (disabled if not set)
BASIC_AUTH_USERNAME=
BASIC_AUTH_PASSWORD=

# 管理者パスワード（香水データ管理ページ用）
# Admin password (for perfume data management page)
ADMIN_PASSWORD=

#===============================================================================
# オプション: WebAuthn設定 / Optional: WebAuthn
# ※ config/infra.json にデフォルト値あり。本番環境では上書き必須
#===============================================================================

# WEBAUTHN_RP_ID=your-domain.com
# WEBAUTHN_ORIGIN=https://your-domain.com
