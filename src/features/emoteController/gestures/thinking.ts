import { GestureDefinition } from './types'

export const thinkingGesture: GestureDefinition = {
  keyframes: [{ duration: 0.6, bones: [] }],
  holdDuration: 2.5,
  vrmaUrl: '/gestures/thinking_anim.vrma',
}
