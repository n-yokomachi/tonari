import { GestureDefinition } from './types'

export const blowAKissGesture: GestureDefinition = {
  keyframes: [{ duration: 0.6, bones: [] }],
  holdDuration: 2.0,
  vrmaUrl: '/gestures/blow_a_kiss.vrma',
}
